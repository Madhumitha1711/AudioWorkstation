{
  "collectionName": "components_shared_audio_assets",
  "info": {
    "displayName": "Audio Asset",
    "icon": "volume-up",
    "description": "A short reference audio clip (e.g. a before/after ear-training example) that can be attached to an assessment question."
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "description": "Short label shown on the play button, e.g. \"Before\", \"After\", \"Example A\"."
    },
    "file": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": ["audios"],
      "description": "Audio clip, uploaded via the S3 provider."
    }
  }
}
