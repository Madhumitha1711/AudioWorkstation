{
  "kind": "collectionType",
  "collectionName": "lessons",
  "info": {
    "singularName": "section",
    "pluralName": "sections",
    "displayName": "Section",
    "description": "A single narrated section within a Chapter, made up of an ordered, mixed set of content blocks (lesson video, image + text, interactive activity, custom embed — see `blocks`). Matches TOPICS[].lessons[] in studio-vr's courseData.js. Renamed from \"Lesson\"; collectionName kept as lessons so existing content isn't orphaned."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "slug": {
      "type": "uid",
      "targetField": "title",
      "required": true,
      "description": "Matches section.id, e.g. \"monitor-types\", \"ported-vs-sealed\"."
    },
    "title": {
      "type": "string",
      "required": true
    },
    "duration": {
      "type": "string",
      "description": "Human-readable duration shown in the UI, e.g. \"4 min\"."
    },
    "order": {
      "type": "integer",
      "default": 0,
      "description": "Position within the parent chapter's section list."
    },
    "blocks": {
      "type": "dynamiczone",
      "components": [
        "course.video-block",
        "course.image-text-block",
        "course.interactive-block",
        "course.custom-embed-block"
      ],
      "description": "Ordered, mixed content for this section — any combination of lesson video, image + text, and interactive-activity blocks, plus custom-embed blocks for frontend components that don't exist yet. The order items appear in here IS the display order studio-vr renders them in — drag to reorder in the admin to change placement."
    },
    "chapter": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::chapter.chapter",
      "inversedBy": "sections"
    }
  }
}
