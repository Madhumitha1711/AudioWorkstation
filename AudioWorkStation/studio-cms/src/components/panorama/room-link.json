{
  "collectionName": "components_panorama_room_links",
  "info": {
    "displayName": "Room Link",
    "icon": "arrows-alt",
    "description": "A doorway arrow from this room to another, matching ROOMS[].links[]."
  },
  "options": {},
  "attributes": {
    "targetRoomSlug": {
      "type": "string",
      "required": true,
      "description": "Slug of the destination studio-room (matches its `slug` field, e.g. \"recording-room\")."
    },
    "yaw": {
      "type": "float",
      "required": true
    },
    "pitch": {
      "type": "float",
      "required": true
    }
  }
}
